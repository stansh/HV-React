import {MENU} from '../shared/menu'

/* export const initialState = {
    snacks: SNACKS
}; */

export const menuReducer = (state = MENU, action) => {
    switch (action.type) {
        default:
          return state;
      }
};